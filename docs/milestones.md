# Milestone evidence

One row per gate. The URL is the snapshot under `builds/<tag>/`. Gate results are honest: passed, failed (with the failing criterion), or missing independent validation.

| Tag | When | URL | Gate result | Screenshot | Clip | Notes |
| --- | --- | --- | --- | --- | --- | --- |
| m1-skeleton | 2026-09-16 10:15 (snapshot written; tag and push pending) | https://jbotoro.github.io/one-more-run/builds/m1-skeleton/ | Skeleton criteria met on localhost: fixed-camera arena, enemies spawn and chase, dash kills, contact costs health, death, score screen, restart, best score persisted, configurator live; 52 tests, Codex review 7/7 fixed. Deployed check and fresh-viewer check not run. | `docs/evidence/m1-skeleton.png` | none | Destination redrawn right after this gate: the arena survivor is retired (a colleague shipped a survivor clone last year). This build is the build-story evidence of the path not taken. No manual code edits. |
| m2-skeleton | 2026-09-16 13:10 (snapshot written; tag and push pending) | https://jbotoro.github.io/one-more-run/builds/m2-skeleton/ | Side-view skeleton. Agent-verifiable criteria met on the deployed root build: start, move, jump, attack, dash, guard and stomper encounters, four connected rooms, death, checkpoint respawn within two seconds, shortcut opens on boss approach and is walkable, Bellkeeper with sweep and stomp telegraphs and phase two; bot closed three death-respawn loops on the deployed build; 55 tests; Codex review 5/6 confirmed and fixed, 1 rejected. Human Gate 1 (2026-09-16 13:20, `docs/playtest.md`): 12 of 13 pass, including three loops by hand and the fresh-viewer 3D read. FAILED: room transitions (exits are unsignposted invisible triggers; leaving Landing on the left arrives in the Gallery on the left, breaking spatial continuity). Direction note: visuals are generic; art direction, level storytelling, and music are as important as gameplay for this iteration. Gate 2a boss first fun: all boxes ticked as a human self-test on this build ("mechanics and difficulty seem fine"); independent validation missing; the look was named the blocker. | `docs/evidence/m2-skeleton-gallery.png`, `docs/evidence/m2-skeleton-deployed.png` | none | Built in about 75 minutes of agent time from the redrawn destination. Level changes for the gate: Gallery dash gap 5.0 to 4.3, cache shaft moved, Belfry Gallery door raised onto a ledge so the shortcut route is clear. No manual code edits. |
| m3-visual | 2026-09-17 08:40 snapshot; tagged and pushed 09:00 (cfe16b7); deployed root and `builds/m3-visual/` both boot and run the bot with a clean console | https://jbotoro.github.io/one-more-run/builds/m3-visual/ | Look C across all four rooms: procedural kit (courses, arches, rails, stairs, passages with light spill, lamps, furnaces, embers), redesigned courier, guard, stomper, and Bellkeeper, graphic effects (arc, burst, chevrons, dust, ridges, lifts), boss bar, display font, title composed from the Landing, procedural audio beds and SFX wired. Human (2026-09-17, `docs/playtest.md`): Landing benchmark approved 6 of 6, largest mismatch the courier (light leak at the shoulders, needle, feet, head-to-torso); Gate 2b exploration 4 of 4; Gate 3 juiced 6 of 6, with the note that victory shows its line and then nothing happens. Listening-check boxes not ticked individually. Claude code review (medium) 8 of 8 confirmed and fixed: same-room respawn no longer rebuilds the room (worst frame after respawn equals baseline), negative tuning defaults tunable, title and pause decor animate on a render clock, attack arc interpolated with the needle, music scheduler skips missed steps after a hidden tab, effect pools cleared on room change, room box geometry cached, burst and arc sliders live. 55 tests, route checks pass, console clean. Deployed check passed. | `docs/evidence/m3-visual-title.png`, `docs/evidence/m3-visual-deployed.png` | none | Codex unavailable (credits until 09-19); review on the Claude side. Seven unreferenced tuning keys removed. No manual code edits. |
| m4-juiced | | | | | | |
| m5-final | | | | | | |

Procedure at each gate (10 minutes): `npm run snapshot -- <tag>`, commit, `git tag <tag>`, push, wait for Pages, open the snapshot URL, take one screenshot and a 15 to 20 second clip, fill the row.
